<h1>PingPong's Result: </h1>
@if (Model.Count == 0) {
  <h3>"please Enter number more than 1"</h3>
}
else
{
  @foreach (var pingPongNumbers in Model){
    <ul>
      <li>@pingPongNumbers.GetNumber()</li>
    </ul>
  
  }
  <a href="/">Play Again!</a>
}
